
const PageSectionHeader = ({text}) => {
    return (
        <section>
            <p className='text-2xl font-semibold uppercase'>{text}</p>
        </section>
    )
};

export default PageSectionHeader;